function array(string) {
    if(string.length === 0 && str.length === 1 && str.length === 2){
      return null;
    }else {
      let stringPop = string.pop();
      let stringShift = string.shift();
      let replaced = sliceTheStRing.replace((/[\s.,%]/g, ''));
      return stringShift;
    
  }
}
